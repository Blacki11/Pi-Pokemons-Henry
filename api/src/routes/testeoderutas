const axios = require('axios')


async function testing(req, res){
  try{
    res.status(200).send("Esto es un testo, y solo funciona para probar la rutas y que no haya bugs al haber hecho la modularizacion")
  }catch(error){
    res.status(500).json(error.message)
  }
}

module.exports = {
    testing,
}